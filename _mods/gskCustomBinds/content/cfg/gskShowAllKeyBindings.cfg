
//show all binds
bind a; bind b; bind c; bind d; bind e; bind f; bind g; bind h; bind i; bind j; bind k; bind l; bind m; bind n; bind o; bind p; bind q; bind r; bind s; bind t; bind u; bind v; bind w; bind x; bind y; bind z; bind 0; bind 1; bind 2; bind 3; bind 4; bind 5; bind 6; bind 7; bind 8; bind 9; bind -; bind =; bind [; bind ]; bind '; bind ,; bind .; bind /; bind ";"

condump
