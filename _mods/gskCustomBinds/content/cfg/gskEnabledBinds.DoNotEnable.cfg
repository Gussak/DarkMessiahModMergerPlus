// my personal keybinds. Do not enable this. It is just a data file for createBindsMenu.sh

//bind g "exec game.cfg" //this may be overkill as it forces a last savegame reload (dont know why tho), but seems to fix any .cfg that failed to load initially (see unifiedGameCfg.sh instead tho).

//unbind m //was notarget cheat //be careful whne using unbindall too
bind m +gskFixMana //(IMPORTANT) enable only if you can control yourself to not cheat (this is just to fix lost mana in case you swap inventories and the mana ring gets unequipped), use the BloodyMana mod instead

bind p +gskFixPauseBugHelper

bind d gskFixShieldsRemove
bind s gskFixShieldsAdd

// for CarefulCombat extra enemies spawner
bind f7 +gskCCnpcSwitch
bind f8 gskCCnpcSpawn
bind z ent_remove

// show detailed entity info
bind t +gskDbgEntInfo

// bloody summon
bind 1 gskSummonSpiderMini
bind 2 gskSummonSpiderRegular
bind 3 gskSummonFacehugger
//bind 4 ""
//bind 5 ""
bind 6 gskSummonCurePoison
bind 7 gskSummonPotionLife
bind 8 gskSummonPotionMana
bind 9 gskSummonPotionStone
bind 0 gskSummonSeagull
bind "-" gskSummonDog
//bind = ""

bind F10 +gskShowSummonKeyBindsHelp

// CarefulCombat
bind x gskCCnpcSpawn_next

// bloody kill
bind k +gskKillTarget

// bloody teleport
bind w +gskTeleMark
bind e +gskTeleRecallSelect
bind r +gskTeleRecallActivate

// dev helpers
bind n +gskHelpPlaceNPCsToggleHelper
bind i +gskShowTriggers
bind u +gskCondump
bind q +gskReloadCfgs

// bloody heal NPC (blood transfusion)
bind h gskNpcFullyHeal

// bloody show NPC route
bind l +gskNpcTelepathy

// bloody teleport NPC
bind a +gskNpcMark
bind y gskNpcMarkedTeleport

